# B21 - Server becomes unjoinable after 80 mins

## Symptoms

- After playing a continuous session for about 80 minutes, players who exit the server cannot rejoin the room.
- `unprocman` seems to have lost connection or lost sync with `unhaunter_dedicated`.
- The client UI does not provide clear error messages about the failure.

## Investigation

- Check if this is related to connection limits. `MAX_CLIENTS` was recently bumped from 4 to 12, but it's unclear if
  connection slots are failing to free up over time, eventually rejecting new/re-joins.
- Check the connection lifecycle and timeouts between `unhub`, `unprocman`, and `unhaunter_dedicated`.
- Set up a test server to run continuous sequential missions for over an hour to reproduce and capture server logs.
- Improve error reporting in the client UI for join failures.

## Findings

- TBD
