# B02 - Salt particles are not despawned

## Symptoms

- `SaltyTrace` particles persist between missions.

## Investigation

- Check `SaltyTrace` component and its spawning logic.
- Find where mission cleanup happens and ensure these are included.

## Findings

- `SaltyTrace` entities are spawned by the ghost movement system with `Replicated`.
- They have a `SaltyTraceTimer` (600s) but don't seem to have `GameSprite` on the server/authority if they are just
  data-only entities there.
- Actually, `hydrate_salty_trace_visuals` adds `GameSprite` on clients/host.
- On the server, they might not have `GameSprite`.
- Action: Ensure `SaltyTrace` entities are cleaned up during mission teardown on the server.

## Testing

needs to be tested. Salty Trace was confirmed. We are pending confirmation of salt pile.

## Review

WRONG CODE. They were deleted inside ghost crates. The ghost has nothing to do with salt - domain violation.

CODE REVERTED.

START AGAIN.

There should be some component markers to tell what sprites are from a mission to be marked for cleanup later.

## Fix Implementation

The following entities were updated to include the `GameSprite` marker on the authority (server/host). This ensures they
are caught by the `teardown_map_entities` system and cleaned up during mission transitions.

### 1. SaltyTrace (Ghost Domain)

- File: [crates/unghost-plugin/src/systems/ghost_ai/movement.rs](crates/unghost-plugin/src/systems/ghost_ai/movement.rs)
- System: `spawn_salty_trace`
- Change: Added `GameSprite` to the spawned entity tuple.

### 2. SaltPile (Gear Logic/Net Domain)

- File: [crates/ungearitems-logic/src/systems/salt.rs](crates/ungearitems-logic/src/systems/salt.rs)
- System: `update_salt_skeleton`
- Change: Added `GameSprite` to the spawned entity tuple (Host-side).

- File: [crates/ungearitems-logic/src/net_state.rs](crates/ungearitems-logic/src/net_state.rs)
- System: `handle_salt_drop_requests`
- Change: Added `GameSprite` to the spawned entity tuple (Server-side).

- Dependency: Added `unboard-core` to `unhaunter-gearitems-logic` Cargo.toml to allow importing `GameSprite`.
